#include <stdio.h>

int main() {

	// 13. Area of rectangle
	float area, length, width;
	scanf("%f %f", &length, &width);
	area = length * width;
	printf("Area of square = %f \n", area);

	return 0;
}

