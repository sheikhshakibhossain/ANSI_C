#include <stdio.h>

int main() {

	// 11. Area of circle
	float radius, pie = 3.1416, area;
	scanf("%f", &radius);
	area = pie * radius * radius;
	printf("Area of circle = %f \n", area);

	return 0;
}

